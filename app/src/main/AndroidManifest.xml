<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.ephealth.chatapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="br.com.ephealth.chatapp.LoginActivity"
            android:parentActivityName="br.com.ephealth.chatapp.StartActivity" />
        <activity android:name="br.com.ephealth.chatapp.MainActivity" />
        <activity
            android:name="br.com.ephealth.chatapp.RegisterActivity"
            android:parentActivityName="br.com.ephealth.chatapp.StartActivity" />
        <activity android:name="br.com.ephealth.chatapp.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>